package ca.aksentiev.emailfilter.filter.api;

/**
 * Loads filter definitions from filters.yml, sorts them
 * (non-stopping before stopping), and executes the filter chain
 * against each incoming email.
 */
public class FilterEngine {
}
