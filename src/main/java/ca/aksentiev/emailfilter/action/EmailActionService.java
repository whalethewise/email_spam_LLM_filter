package ca.aksentiev.emailfilter.action;

/**
 * Executes email actions (move, delete, flag, forward, tag)
 * as determined by filter results. Coordinates with IMAP
 * and delegates audit logging.
 */
public class EmailActionService {
}
